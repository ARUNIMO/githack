import { Story } from '@storybook/react';
import { FilterProps } from './Filter';
declare const _default: {
    title: string;
    component: (props: FilterProps) => JSX.Element;
    argTypes: {
        onDelete: {
            action: string;
        };
    };
};
export default _default;
export declare const Demo: Story<FilterProps>;
