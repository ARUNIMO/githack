import * as React from 'react';
import { ProjectPreviewActionProps } from '../ProjectPreviewAction';
declare type Size = 'medium' | 'large';
interface ProjectPreviewProps {
    /**
     * The size of the project preview card.
     * @defaultValue `medium`
     */
    size?: Size;
    /** The title of the project. */
    name: string;
    /** The elevator pitch or tagline. */
    elevatorPitch: string;
    /** URL of the project image or an image component. */
    image?: string | React.ReactNode;
    /** Team members info. */
    teamMembers?: {
        /** Member's Devfolio username. */
        username: string;
        /** Member's avatar URL. */
        avatar: string;
    }[];
    support: ProjectPreviewActionProps;
    upvote: ProjectPreviewActionProps;
}
declare const ProjectPreview: (props: ProjectPreviewProps) => JSX.Element;
export { ProjectPreview };
export type { ProjectPreviewProps };
